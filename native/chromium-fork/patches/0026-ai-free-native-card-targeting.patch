diff --git a/chrome/browser/ui/views/frame/ai_free_page_automation.cc b/chrome/browser/ui/views/frame/ai_free_page_automation.cc
index 9e10c43..b88a43e 100644
--- a/chrome/browser/ui/views/frame/ai_free_page_automation.cc
+++ b/chrome/browser/ui/views/frame/ai_free_page_automation.cc
@@ -69,6 +69,7 @@ std::string ObserveScript(const base::DictValue& command) {
     const keyword=String(a.keyword||'').toLowerCase();
     const tagFilter=String(a.tag||'').toLowerCase();
     const kindFilter=String(a.filter||'').toLowerCase();
+    const selectorFilter=String(a.selector||'').trim();
     const interactive='a[href],button,input,textarea,select,summary,[role="button"],[role="link"],[role="checkbox"],[role="menuitem"],[contenteditable="true"]';
     const items=[]; let matched=0;
     const selector=(el)=>{if(el.id)return '#'+CSS.escape(el.id);const n=el.getAttribute('name');if(n)return el.tagName.toLowerCase()+'[name="'+CSS.escape(n)+'"]';let s=el.tagName.toLowerCase();const p=el.parentElement;if(p){const same=[...p.children].filter(x=>x.tagName===el.tagName);if(same.length>1)s+=':nth-of-type('+(same.indexOf(el)+1)+')';}return s;};
@@ -77,6 +78,6 @@ std::string ObserveScript(const base::DictValue& command) {
     const visible=(el,r)=>{const c=getComputedStyle(el);if(c.display==='none'||c.visibility==='hidden'||Number(c.opacity)===0)return false;if(r.width<1||r.height<1)return false;return r.bottom>0&&r.right>0&&r.top<innerHeight&&r.left<innerWidth;};
     const roots=[document]; const seen=new Set();
-    for(let ri=0;ri<roots.length;ri++){const root=roots[ri];if(seen.has(root))continue;seen.add(root);for(const el of root.querySelectorAll('*')){if(el.shadowRoot)roots.push(el.shadowRoot);if(el.tagName==='IFRAME'){try{if(el.contentDocument)roots.push(el.contentDocument);}catch(_){}}const r=el.getBoundingClientRect();if(!visible(el,r))continue;let kind='';const tag=el.tagName.toLowerCase();if(el.matches(interactive)||getComputedStyle(el).cursor==='pointer')kind='interactive';else if(a.includeMedia!==false&&/^(img|video|audio)$/.test(tag))kind='media';else if(a.includeText!==false&&el.children.length===0&&String(el.innerText||el.textContent||'').trim())kind='text';if(!kind)continue;const text=String(el.innerText||el.textContent||el.value||el.getAttribute('aria-label')||'').trim().replace(/\s+/g,' ').slice(0,500);if(keyword&&!text.toLowerCase().includes(keyword))continue;if(tagFilter&&tag!==tagFilter)continue;if(kindFilter&&kind!==kindFilter)continue;matched++;if(items.length>=limit)continue;const item={id:'e'+matched,kind,tag,selector:selector(el),text,name:el.getAttribute('name')||'',placeholder:el.getAttribute('placeholder')||'',ariaLabel:el.getAttribute('aria-label')||'',value:/password/i.test(el.type||'')?'':String(el.value||'').slice(0,300),disabled:!!el.disabled,x:Math.round(r.x),y:Math.round(r.y),width:Math.round(r.width),height:Math.round(r.height)};if(tag==='a'&&/^https?:/i.test(el.href||'')){item.href=String(el.href).slice(0,2048);item.downloadUrl=item.href;const n=el.getAttribute('download');if(n)item.downloadFilename=String(n).slice(0,160);}items.push(item);}}
+    for(let ri=0;ri<roots.length;ri++){const root=roots[ri];if(seen.has(root))continue;seen.add(root);for(const el of root.querySelectorAll('*')){if(el.shadowRoot)roots.push(el.shadowRoot);if(el.tagName==='IFRAME'){try{if(el.contentDocument)roots.push(el.contentDocument);}catch(_){}}const r=el.getBoundingClientRect();if(!visible(el,r))continue;let kind='';const tag=el.tagName.toLowerCase();if(el.matches(interactive)||getComputedStyle(el).cursor==='pointer')kind='interactive';else if(a.includeMedia!==false&&/^(img|video|audio)$/.test(tag))kind='media';else if(a.includeText!==false&&el.children.length===0&&String(el.innerText||el.textContent||'').trim())kind='text';if(!kind)continue;if(selectorFilter){try{if(!el.matches(selectorFilter))continue;}catch(_){return {success:false,error:'CSS selector 无效',errorCode:'SELECTOR_INVALID'};}}const text=String(el.innerText||el.textContent||el.value||el.getAttribute('aria-label')||'').trim().replace(/\s+/g,' ').slice(0,500);if(keyword&&!text.toLowerCase().includes(keyword))continue;if(tagFilter&&tag!==tagFilter)continue;if(kindFilter&&kind!==kindFilter)continue;matched++;if(items.length>=limit)continue;const item={id:'e'+matched,kind,tag,selector:selector(el),text,name:el.getAttribute('name')||'',placeholder:el.getAttribute('placeholder')||'',ariaLabel:el.getAttribute('aria-label')||'',value:/password/i.test(el.type||'')?'':String(el.value||'').slice(0,300),disabled:!!el.disabled,x:Math.round(r.x),y:Math.round(r.y),width:Math.round(r.width),height:Math.round(r.height)};if(tag==='a'&&/^https?:/i.test(el.href||'')){item.href=String(el.href).slice(0,2048);item.downloadUrl=item.href;const n=el.getAttribute('download');if(n)item.downloadFilename=String(n).slice(0,160);}items.push(item);}}
     const downloadLinks=items.filter(i=>i.downloadUrl).map(i=>({url:i.downloadUrl,text:i.text||'',selector:i.selector,...(i.downloadFilename?{filename:i.downloadFilename}:{})}));
     return {success:true,url:location.href,title:document.title,viewport:{width:innerWidth,height:innerHeight,scrollX,scrollY},items,downloadLinks,downloadLinkCount:downloadLinks.length,count:items.length,totalMatched:matched,truncated:matched>items.length};
   } catch(e) { return {success:false,error:String(e&&e.message||e),errorCode:'OBSERVE_FAILED'}; } })())JS"});
@@ -89,8 +90,9 @@ std::string ActionScript(const base::DictValue& command) {
                        CommandJson(command),
                        R"JS(; const action=String(a.action||'');
     const roots=()=>{const out=[document];for(let i=0;i<out.length;i++){for(const e of out[i].querySelectorAll('*')){if(e.shadowRoot)out.push(e.shadowRoot);if(e.tagName==='IFRAME'){try{if(e.contentDocument)out.push(e.contentDocument);}catch(_){}}}}return out;};
-    const find=()=>{for(const root of roots()){if(a.selector){try{const e=root.querySelector(a.selector);if(e)return e;}catch(_){}}const needle=String(a.text||'').trim();if(needle&&action!=='type'){for(const e of root.querySelectorAll('button,a,input,textarea,select,[role="button"],[contenteditable="true"]')){if(String(e.innerText||e.value||e.getAttribute('aria-label')||'').trim().includes(needle))return e;}}}return action==='press_key'?document.activeElement:null;};
-    if(action==='wait')return find()?{success:true,action,found:true}:{success:false,action,error:'等待元素中',errorCode:'WAIT_PENDING'};
+    const nth=Math.max(0,Math.floor(Number(a.nth)||0));
+    const find=()=>{for(const root of roots()){if(a.selector){try{const matches=root.querySelectorAll(a.selector);if(matches[nth])return matches[nth];}catch(_){}}const needle=String(a.targetText||(action==='type'?'':a.text)||'').trim();if(needle){const matches=[...root.querySelectorAll('button,a,input,textarea,select,[role="button"],[contenteditable="true"]')].filter(e=>String(e.innerText||e.value||e.getAttribute('aria-label')||'').trim().includes(needle));if(matches[nth])return matches[nth];}}return action==='press_key'?document.activeElement:null;};
+    if(action==='wait'){const found=!!find();return found!==!!a.hidden?{success:true,action,found}:{success:false,action,error:'等待条件尚未满足',errorCode:'WAIT_PENDING'};}
     const el=find();if(!el)return {success:false,action,error:'未找到目标元素',errorCode:'ELEMENT_NOT_FOUND'};el.scrollIntoView({block:'center',inline:'center'});el.focus();
     if(action==='type'){if(typeof el.select==='function')el.select();return {success:true,action,selector:a.selector||'',nativeKeyboard:true};}
     if(action==='press_key')return {success:true,action,key:String(a.key||a.text||'Enter'),nativeKeyboard:true};
