diff --git a/chrome/browser/ui/views/extensions/extension_popup.cc b/chrome/browser/ui/views/extensions/extension_popup.cc
index 03f23d53..8c61cc21 100644
--- a/chrome/browser/ui/views/extensions/extension_popup.cc
+++ b/chrome/browser/ui/views/extensions/extension_popup.cc
@@ -5,6 +5,7 @@
 #include "chrome/browser/ui/views/extensions/extension_popup.h"
 
 #include "base/check.h"
+#include "base/command_line.h"
 #include "base/functional/bind.h"
 #include "base/memory/raw_ptr.h"
 #include "chrome/browser/devtools/devtools_window.h"
@@ -12,6 +13,7 @@
 #include "chrome/browser/ui/browser.h"
 #include "chrome/browser/ui/views/extensions/security_dialog_tracker.h"
 #include "chrome/browser/ui/views/frame/browser_view.h"
+#include "chrome/common/chrome_switches.h"
 #include "components/javascript_dialogs/app_modal_dialog_queue.h"
 #include "components/web_modal/web_modal_utils.h"
 #include "content/public/browser/browser_context.h"
@@ -285,8 +287,15 @@ ExtensionPopup::ExtensionPopup(
   extension_view_->SetContainer(this);
   extension_view_->Init();
 
-  // See comments in OnWidgetActivationChanged().
-  set_close_on_deactivate(false);
+  // Ordinary Chromium keeps extension popups open while switching to another
+  // application and closes them when another widget in the browser tree is
+  // activated. In AI-FREE child-window mode the Browser HWND belongs to the
+  // Electron host's native window tree, so that widget-tree activation signal
+  // is not reliable. Close on native deactivation in that mode so extension
+  // popups cannot remain orphaned after the user clicks elsewhere.
+  set_close_on_deactivate(
+      base::CommandLine::ForCurrentProcess()->GetSwitchValueASCII(
+          switches::kHsEmbedMode) == "child-window");
 
   scoped_devtools_observation_ =
       std::make_unique<ScopedDevToolsAgentHostObservation>(this);
diff --git a/chrome/browser/ui/views/toolbar/toolbar_action_hover_card_controller.cc b/chrome/browser/ui/views/toolbar/toolbar_action_hover_card_controller.cc
index 3c55717f..f4fbb05f 100644
--- a/chrome/browser/ui/views/toolbar/toolbar_action_hover_card_controller.cc
+++ b/chrome/browser/ui/views/toolbar/toolbar_action_hover_card_controller.cc
@@ -5,6 +5,7 @@
 #include "chrome/browser/ui/views/toolbar/toolbar_action_hover_card_controller.h"
 
 #include "base/callback_list.h"
+#include "base/command_line.h"
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
 #include "base/memory/raw_ptr.h"
@@ -15,8 +16,10 @@
 #include "chrome/browser/ui/views/extensions/extensions_toolbar_desktop.h"
 #include "chrome/browser/ui/views/toolbar/toolbar_action_hover_card_bubble_view.h"
 #include "chrome/browser/ui/views/toolbar/toolbar_action_view.h"
+#include "chrome/common/chrome_switches.h"
 #include "content/public/browser/web_contents.h"
 #include "extensions/common/extension_features.h"
+#include "ui/display/screen.h"
 #include "ui/events/event_observer.h"
 #include "ui/views/bubble/bubble_anchor.h"
 #include "ui/views/event_monitor.h"
@@ -27,6 +30,8 @@ namespace {
 
 constexpr base::TimeDelta kTriggerDelay = base::Milliseconds(300);
 constexpr base::TimeDelta kHoverCardSlideDuration = base::Milliseconds(200);
+constexpr base::TimeDelta kEmbeddedHoverExitPollInterval =
+    base::Milliseconds(150);
 
 }  // namespace
 
@@ -268,6 +273,14 @@ void ToolbarActionHoverCardController::ShowHoverCard(
   }
 
   CreateHoverCard(target_action_view_);
+  if (base::CommandLine::ForCurrentProcess()->GetSwitchValueASCII(
+          switches::kHsEmbedMode) == "child-window") {
+    embedded_hover_exit_watchdog_.Start(
+        FROM_HERE, kEmbeddedHoverExitPollInterval,
+        base::BindRepeating(
+            &ToolbarActionHoverCardController::CheckEmbeddedHoverExit,
+            weak_ptr_factory_.GetWeakPtr()));
+  }
   UpdateHoverCardContent(target_action_view_);
   slide_animator_->UpdateTargetBounds(views::BubbleAnchor(target_action_view_));
   // TODO(crbug.com/40857356): Do we need to fix widget stack order? Revisit
@@ -282,6 +295,7 @@ void ToolbarActionHoverCardController::ShowHoverCard(
 }
 
 void ToolbarActionHoverCardController::HideHoverCard() {
+  embedded_hover_exit_watchdog_.Stop();
   if (!hover_card_ || hover_card_->GetWidget()->IsClosed()) {
     return;
   }
@@ -304,6 +318,18 @@ void ToolbarActionHoverCardController::HideHoverCard() {
   fade_animator_->FadeOut();
 }
 
+void ToolbarActionHoverCardController::CheckEmbeddedHoverExit() {
+  if (!hover_card_ || !target_action_view_) {
+    embedded_hover_exit_watchdog_.Stop();
+    return;
+  }
+
+  const gfx::Point cursor = display::Screen::Get()->GetCursorScreenPoint();
+  if (!target_action_view_->GetBoundsInScreen().Contains(cursor)) {
+    UpdateHoverCard(nullptr, ToolbarActionHoverCardUpdateType::kHover);
+  }
+}
+
 bool ToolbarActionHoverCardController::ShouldShowImmediately(
     const ToolbarActionView* action_view) const {
   // If less than `kShowWithoutDelayTimeBuffer` time has passed since the hover
@@ -370,6 +396,7 @@ void ToolbarActionHoverCardController::OnSlideAnimationComplete(
 void ToolbarActionHoverCardController::OnViewIsDeleting(
     views::View* observed_view) {
   if (hover_card_ == observed_view) {
+    embedded_hover_exit_watchdog_.Stop();
     delayed_show_timer_.Stop();
     hover_card_observation_.Reset();
     event_sniffer_.reset();
diff --git a/chrome/browser/ui/views/toolbar/toolbar_action_hover_card_controller.h b/chrome/browser/ui/views/toolbar/toolbar_action_hover_card_controller.h
index 3a583458..5523843d 100644
--- a/chrome/browser/ui/views/toolbar/toolbar_action_hover_card_controller.h
+++ b/chrome/browser/ui/views/toolbar/toolbar_action_hover_card_controller.h
@@ -51,6 +51,7 @@ class ToolbarActionHoverCardController : public views::ViewObserver {
   void CreateHoverCard(ToolbarActionView* action_view);
   void ShowHoverCard(bool is_initial, const ToolbarActionView* action_view);
   void HideHoverCard();
+  void CheckEmbeddedHoverExit();
 
   bool ShouldShowImmediately(const ToolbarActionView* action_view) const;
 
@@ -102,6 +103,7 @@ class ToolbarActionHoverCardController : public views::ViewObserver {
 
   // Ensure that this timer is destroyed before anything else is cleaned up.
   base::OneShotTimer delayed_show_timer_;
+  base::RepeatingTimer embedded_hover_exit_watchdog_;
   base::WeakPtrFactory<ToolbarActionHoverCardController> weak_ptr_factory_{
       this};
 };
