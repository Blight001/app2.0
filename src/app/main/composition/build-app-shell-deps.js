// createAppShell 依赖装配（阶段 2D-3，自 bootstrap.js 原样迁出）。
// late.* 是 tabManager/auth/appShell 创建后才可用的晚绑定访问器，由 bootstrap 提供。
'use strict';

const { createAuthCookie } = require('../lib/auth-cookie');
const { registerIPC } = require('../ipc/register');
const { createHttpClient } = require('../lib/http-client');
const { stopClashMiniProcess } = require('../ipc/register/clash-mini-core');
const { initDownloadPrefs, downloadOrSaveMedia } = require('../utils/download');
const { attachContextMenu, clearInjectionRecord, shortcutManager } = require('../utils/removeWatermark');
const { injectZoomWheelListener } = require('../utils/zoom');
const { checkDesktopShortcutAndPrompt } = require('../utils/shortcut');
const { resolveAppIconPath } = require('../utils/app-icon');
const { initializeAccountCleanup } = require('../utils/accountCleanup');
const {
  DREAM_TARGET_URL,
  setDreamTargetUrl,
  getDreamTargetUrl,
  setRuntimeTcpConfig,
  setRuntimeServerBase,
  getStorePath,
  getServerBase,
} = require('../config');

const FIXED_ICON_RELATIVE_PATH = 'src/assets/logo.ico';

function buildAppShellDeps({
  electron: { app, fs, path, BrowserWindow, WebContentsView, dialog, Menu, Tray, screen },
  services,
  refreshAllowedPlatformsAndNotify,
  resetRuntimeTutorialUrlState,
  extIdBySession,
  late,
  getAppShell,
}) {
  const {
    appRuntime,
    tabs,
    browserRuntimeManager,
    browserAutomationBridge,
    licenseCache,
    browserPartitionCleaner,
    state,
    applyPluginSettings,
    isDevMode,
    sendToSide,
    getAppConsoleHistory,
    getDebugConsoleHistory,
    appUpdater,
    licenseStore,
    tabHelpers,
    runtimeHelpers,
    extensionManager,
    accountStorage,
    http,
    APP_DISPLAY_NAME,
  } = services;

  return {
    app,
    fs,
    path,
    BrowserWindow,
    WebContentsView,
    browserRuntimeManager,
    browserAutomationBridge,
    dialog,
    Menu,
    Tray,
    screen,
    logger: console,
    FIXED_ICON_RELATIVE_PATH,
    resolveAppIconPath,
    APP_DISPLAY_NAME,
    state,
    createAuthCookie,
    createHttpClient,
    loadTranslateExtension: runtimeHelpers.loadTranslateExtension,
    attachContextMenu,
    initDownloadPrefs,
    injectZoomWheelListener,
    checkDesktopShortcutAndPrompt,
    initializeAccountCleanup,
    refreshAllowedPlatformsAndNotify,
    resetRuntimeTutorialUrlState,
    registerIPC,
    stopClashMiniProcess,
    getStorePath,
    getServerBase,
    getDreamTargetUrl,
    setDreamTargetUrl,
    DREAM_TARGET_URL,
    getCurrentPlatformLabel: licenseStore.getCurrentPlatformLabel,
    readStoreConfigSafe: licenseStore.readStoreConfigSafe,
    writeStoreConfigSafe: licenseStore.writeStoreConfigSafe,
    appendLicenseRecord: licenseStore.appendLicenseRecord,
    applyPluginSettings,
    computeDeviceId: runtimeHelpers.computeDeviceId,
    getAuth: late.getAuth,
    setAuth: late.setAuth,
    getAddTab: late.getAddTab,
    getOpenTutorialTab: late.getOpenTutorialTab,
    getSyncTutorialTabUrl: late.getSyncTutorialTabUrl,
    getSwitchTab: late.getSwitchTab,
    getCloseTab: late.getCloseTab,
    getReorderTab: late.getReorderTab,
    getRenameTab: late.getRenameTab,
    getSetTabBrowserSettings: late.getSetTabBrowserSettings,
    getSetZoom: late.getSetZoom,
    getRefreshActiveTabToUrl: late.getRefreshActiveTabToUrl,
    getRefreshActiveTab: late.getRefreshActiveTab,
    getRefreshTab: late.getRefreshTab,
    getAddExternalApp: late.getAddExternalApp,
    listAvailableSoftware: () => services.softwareCatalog.listAvailable(),
    updateTabs: tabHelpers.updateTabs,
    getActiveWC: tabHelpers.getActiveWC,
    toggleSidebar: tabHelpers.toggleSidebar,
    sendToSide,
    startAppUpdate: appUpdater.startAppUpdate,
    handleServerUpdateCommand: appUpdater.handleServerUpdateCommand,
    cleanupUpdateStorageRoot: appUpdater.cleanupUpdateStorageRoot,
    getAppVersion: () => app.getVersion(),
    getTabs: () => tabs,
    getMainWindow: appRuntime.getMainWindow,
    setMainWindow: appRuntime.setMainWindow,
    getSideView: appRuntime.getSideView,
    setSideView: appRuntime.setSideView,
    getControlPanelWindow: appRuntime.getControlPanelWindow,
    setControlPanelWindow: appRuntime.setControlPanelWindow,
    getConsoleWindow: appRuntime.getConsoleWindow,
    setConsoleWindow: appRuntime.setConsoleWindow,
    getLicenseWindow: appRuntime.getLicenseWindow,
    setLicenseWindow: appRuntime.setLicenseWindow,
    getActiveTabId: appRuntime.getActiveTabId,
    setActiveTabId: appRuntime.setActiveTabId,
    getExtPopupWin: appRuntime.getExtPopupWin,
    setExtPopupWin: appRuntime.setExtPopupWin,
    getIsSidebarVisible: appRuntime.getIsSidebarVisible,
    setIsSidebarVisible: appRuntime.setIsSidebarVisible,
    getIsMainBootstrapped: appRuntime.getIsMainBootstrapped,
    setIsMainBootstrapped: appRuntime.setIsMainBootstrapped,
    getIsSwitchingToLicense: appRuntime.getIsSwitchingToLicense,
    setIsSwitchingToLicense: appRuntime.setIsSwitchingToLicense,
    getLatestAllowedPlatforms: appRuntime.getLatestAllowedPlatforms,
    setLatestAllowedPlatforms: appRuntime.setLatestAllowedPlatforms,
    licenseCache,
    getGlobalHttpClient: appRuntime.getGlobalHttpClient,
    setGlobalHttpClient: appRuntime.setGlobalHttpClient,
    cleanupBrowserSessionData: browserPartitionCleaner.cleanupBrowserSessionData,
    purgeBrowserSessionData: browserPartitionCleaner.purgeBrowserSessionData,
    cleanupAllBrowserSessionData: browserPartitionCleaner.cleanupAllBrowserSessionData,
    cleanupBrowserPartitionsRootDir: browserPartitionCleaner.cleanupBrowserPartitionsRootDir,
    accountStorage,
    shortcutManager,
    extIdBySession,
    clearInjectionRecord,
    getAppConsoleHistory,
    getDebugConsoleHistory,
    statePluginGetter: () => state.pluginSettings,
    setRuntimeTcpConfig,
    setRuntimeServerBase,
    httpGetUniversal: http.httpGetUniversal,
    postJson: http.postJson,
    getJson: http.getJson,
    downloadOrSaveMedia,
    extensionManager,
    createDevConsoleWindow: () => getAppShell()?.createDevConsoleWindow?.(),
    isDevMode,
  };
}

module.exports = { buildAppShellDeps };
